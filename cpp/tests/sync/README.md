Tests for `cpp/sync/*` (currently `SyncOrchestrator`) go here, mirroring the
main `cpp/` layout. `SyncOrchestrator::triggerSync` is still an unimplemented
stub (TASK-012), so there's nothing to test yet beyond "it throws". Add real
coverage here once sync is implemented.
