// Real device: needs the Mac's LAN IP, not localhost — edit this line locally.
// iOS Simulator can use localhost; Android emulator needs 10.0.2.2 instead.
// Start the server first: `npm run dev -w salve-db-server` from the repo root.
export const SYNC_SERVER_BASE_URL = 'http://localhost:4000';
