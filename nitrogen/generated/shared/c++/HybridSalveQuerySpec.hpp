///
/// HybridSalveQuerySpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `QueryResult` to properly resolve imports.
namespace margelo::nitro::salvedb { struct QueryResult; }

#include "QueryResult.hpp"
#include <NitroModules/Promise.hpp>
#include <string>
#include <NitroModules/Null.hpp>
#include <NitroModules/ArrayBuffer.hpp>
#include <variant>
#include <vector>

namespace margelo::nitro::salvedb {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `SalveQuery`
   * Inherit this class to create instances of `HybridSalveQuerySpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridSalveQuery: public HybridSalveQuerySpec {
   * public:
   *   HybridSalveQuery(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridSalveQuerySpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridSalveQuerySpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridSalveQuerySpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<QueryResult>> execute(const std::string& sql, const std::vector<std::variant<nitro::NullType, bool, std::shared_ptr<ArrayBuffer>, std::string, double>>& params) = 0;
      virtual std::shared_ptr<Promise<void>> beginTransaction() = 0;
      virtual std::shared_ptr<Promise<void>> commit() = 0;
      virtual std::shared_ptr<Promise<void>> rollback() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "SalveQuery";
  };

} // namespace margelo::nitro::salvedb
